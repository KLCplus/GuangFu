# Web Frontend

Vue 3 + Vite + TypeScript Web 端骨架，包含用户端、管理端路由，Axios API 层、Pinia 状态和 ECharts 示例图表。

```bash
npm install
npm run dev
```

访问 `http://localhost:5173`。Vite 会将 `/api` 和 `/openapi` 代理到 `http://localhost:8080`。

生产构建：

```bash
npm run build
```
