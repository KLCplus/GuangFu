来到模型界面

1.首先分类有错
功率时序模型为：
PatchTST、DLinear、iTransformer、TimeXer、TimeMixer、TSMixer、Transformer
云图时序模型为：
SimVP+GSTA、TAU、PredRNN、PredRNN++、ConvLSTM、E3D-LSTM、SwinLSTM、sunset
多模态融合模型为：
CNN+LSTM、3D-CNN+LSTM、CNN+MLP、ConvLSTM+LSTM

2.输入逻辑有错
对于功率模型
只需要输入功率数据文件即可，不需要选择云图文件夹即可进行预测
对于云图时序模型
只用到云图文件夹即可进行预测，不需要用到功率数据文件
但是，要求必须传入功率数据文件和云图文件夹才能开始 预测
对于多模态模型
同时需要云图文件夹和功率数据文件

此处需要云图文件夹是指需要6个时间步，1个时间步5分钟，总共6张历史云图
此处需要功率文件是指需要30个时间步，1个时间步1分钟，总共30是历史功率数据，以excel形式或者csv形式呈现
功率预测结果是个折线图，展示前30个时间步的功率和未来6个时间步的功率，也就是说总共展示60分钟的功率，请注意历史功率和预测功率的线请使用不同颜色标注，而且再使用另一种颜色把对应时间的真实功率曲线也标出

3.平均功率
0.0 kW
峰值功率
0.0 kW直接给我删去

4.云图预测文件夹请你实现它的前后端代码的逻辑